stages:
  - build
  - test
  - deploy

variables:
  PRODUCTION: $PRODUCTION
  ALLOWED_ORIGINS: $ALLOWED_ORIGINS
  PRODUCTION_DB_URL: $PRODUCTION_DB_URL

build-job:
  stage: build
  script:
    - docker build -t my-backend-image .

test-job:
  stage: test
  script:
    # Placeholder for future test execution
    # Original command: docker run --rm my-backend-image pytest -s -v
    - echo "Testing"
    - echo "PRODUCTION = $PRODUCTION"

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
