stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - docker build -t my-backend-image .

test-job:
  stage: test
  script:
    # - docker run --rm my-backend-image pytest -s -v
    - echo "Tests are currently skipped. Placeholder for future test execution."

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
